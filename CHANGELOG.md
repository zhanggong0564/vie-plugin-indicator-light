# Changelog

## [Unreleased]

## [0.1.6] - 2026-09-15（Git tag: `v0.1.6`）

- 修复悬挂钥匙扩大检测框、中心偏移导致整组无法匹配：保留快速与 ORB 中心匹配，
  仅在至少五个位置且仅剩一个未匹配时，允许配准后唯一、重叠充分的扩展框恢复对应。
  拒绝歧义、过大框及覆盖相邻位置；保留缺失失败和全部位置的相似度判定。

- 指示灯默认相似度阈值由 0.65 提高到 0.80，与生产配置一致；同步接口文档，保留环境变量覆盖能力。

## [0.1.5] - 2026-09-14（Git tag: `v0.1.5`）

- 移除从未参与推理的 `rec_conf_threshold`、`json_path` 和 `cache_enabled`
  遗留配置，并清理 Chroma 存储中未使用的解析转发方法，避免无效接口误导调试。
- 区分灯位数量不一致与数量一致但布局不可靠的错误信息，避免输出 `11!=11`
  等误导性信息。
- 指示灯比对改为注册灯位驱动的几何匹配，支持检测框错序、缩放和平移；异常布局使用 ORB/RANSAC 配准兜底，远离注册灯位的额外误检框不再直接导致失败。
- 注册向量缓存新增归一化框、图像宽高和布局版本；旧格式缓存自动失效并在首次使用时重新生成。
- 数据回流新增 `unmatch` 分类，单独保存注册图与待测图指示灯数量不一致的样本；实际下载的注册图按日期、物料号和注册 ID 归档到 `registered` 目录，并保留既有 `review` 分类。
- ROI 检测模型从 YOLO 切换为单类 `rfdetr-small.onnx`，后续排序、裁图、特征识别和比对契约保持不变。
- 回流目录在基础物料号下新增版本层级，按 `物料号/1`、`物料号/2` 分离注册图和检测样本。

本插件变更记录遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)
规范，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

- 补充场景响应字段语义、整体判定与空结果规则，以及含明细的通过、不通过和边界响应示例；文档仅声明实际支持的 PASS/FAIL。

- 修复接口文档：声明实际请求模型、参数规则及可校验示例，由框架生成 Swagger 参数说明和响应示例。

### 修复

- 使用不可变对象替换方式设置回流文件时间戳，修复修改冻结回流目标时的运行错误。

### 变更

- 请求参数与 AICameraModel 复用框架公共结构，保持原 JSON 契约不变。
- 场景配置迁移到框架 `SceneSettings`，统一 `INDICATOR_` 环境变量解析及范围校验。

## [0.1.4] - 2026-08-07

- 注册来源指纹变化并重新下载 `ModelFile` 时覆盖同注册 ID 的历史归档图，避免
  新向量缓存继续配合旧注册图片；补充注册记录选择、缓存命中/刷新和归档日志。
- `unmatch` 样本继续进入独立回流目录，调用统计统一按 `ng` 记录。

## [0.1.3] - 2026-08-06

- 修复 `register=true` 未触发注册参考图和向量缓存刷新的问题；注册信息变化时
  重新下载 `ModelFile`、更新特征并覆盖同一注册 ID 的当日归档图。

## [0.1.2] - 2026-07-28

### 新增

- 新增注册参考图的受控下载、注册向量解析与持久化缓存。
- 新增 Chroma 向量存储及缓存模型，支持注册向量重载。

### 修复

- 加强注册参考图下载校验与错误分类，限制下载超时、响应大小和访问范围。
- 补充 ChromaDB 运行依赖声明，确保默认向量缓存配置可用。
- 示例在注册图与待检测图 ROI 数量不一致时仍输出双图检测框拼接结果。

### 变更

- 将 Chroma generation 元数据解析与数值校验从持久化编排中拆分，保留原存储接口。
- 场景注册迁移到框架 `ScenarioRegistry`，检测与动态 batch 特征模型统一由框架
  runner factory 创建并注入。
- 组合管线补齐双模型关闭契约；初始化任一步失败时回滚已创建的 runner。
- 示例切换到新场景注册入口，并在退出时释放模型资源。
- 同步示例和配置中的模型路径，采用 `weights/{scene}/{task}_{arch}_v{N}` 命名规范。

## [0.1.1] - 2026-07-15

### 性能

- 将单张图片的全部指示灯 ROI 合并为一次动态 batch 特征推理。

### 变更

- 指示灯识别模型默认切换到动态 batch 的 `rec_v3.onnx`，固定 batch 旧模型不再接受。
- 新生成的特征统一为 `[ROI][D]`，并兼容读取旧的 `[ROI][1][D]` 注册向量。

## [0.1.0] - 2026-05-30

### 新增

- 将指示灯旧场景迁移为 `vie-plugin-indicator-light` 独立插件。
- 新增插件配置、路由、业务逻辑、单元测试和运行示例。
- 新增框架与插件二进制 wheel 构建配置。
