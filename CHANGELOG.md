# Changelog

## Unreleased

- 数据回流新增 `unmatch` 分类，单独保存注册图与待测图指示灯数量不一致的样本；实际下载的注册图按日期、物料号和注册 ID 归档到 `registered` 目录，并保留既有 `review` 分类。

本插件变更记录遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)
规范，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 修复

- 使用不可变对象替换方式设置回流文件时间戳，修复修改冻结回流目标时的运行错误。

### 变更

- 请求参数与 AICameraModel 复用框架公共结构，保持原 JSON 契约不变。
- 场景配置迁移到框架 `SceneSettings`，统一 `INDICATOR_` 环境变量解析及范围校验。

## [0.1.2] - 2026-07-28

### 新增

- 新增注册参考图的受控下载、注册向量解析与持久化缓存。
- 新增 Chroma 向量存储及缓存模型，支持注册向量重载。

### 修复

- 加强注册参考图下载校验与错误分类，限制下载超时、响应大小和访问范围。
- 补充 ChromaDB 运行依赖声明，确保默认向量缓存配置可用。
- 示例在注册图与待检测图 ROI 数量不一致时仍输出双图检测框拼接结果。

### 变更

- 将 Chroma generation 元数据解析与数值校验从持久化编排中拆分，保留原存储接口。
- 场景注册迁移到框架 `ScenarioRegistry`，检测与动态 batch 特征模型统一由框架
  runner factory 创建并注入。
- 组合管线补齐双模型关闭契约；初始化任一步失败时回滚已创建的 runner。
- 示例切换到新场景注册入口，并在退出时释放模型资源。
- 同步示例和配置中的模型路径，采用 `weights/{scene}/{task}_{arch}_v{N}` 命名规范。

## [0.1.1] - 2026-07-15

### 性能

- 将单张图片的全部指示灯 ROI 合并为一次动态 batch 特征推理。

### 变更

- 指示灯识别模型默认切换到动态 batch 的 `rec_v3.onnx`，固定 batch 旧模型不再接受。
- 新生成的特征统一为 `[ROI][D]`，并兼容读取旧的 `[ROI][1][D]` 注册向量。

## [0.1.0] - 2026-05-30

### 新增

- 将指示灯旧场景迁移为 `vie-plugin-indicator-light` 独立插件。
- 新增插件配置、路由、业务逻辑、单元测试和运行示例。
- 新增框架与插件二进制 wheel 构建配置。
